/* Placeholder JSZip build.
 * NOTE: Replace this file with the official JSZip 3.10.1 minified build.
 */
(function (global) {
  function missing() {
    return Promise.reject(new Error("JSZip vendor file is a placeholder. Replace assets/vendor/jszip.min.js with the official build."));
  }
  global.JSZip = {
    loadAsync: missing,
  };
})(typeof window !== "undefined" ? window : this);
